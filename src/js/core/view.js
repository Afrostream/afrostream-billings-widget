export default class view {
  constructor (host, path) {
    this.host = host
    this.path = path
  }

  getHost () {
    return this.host
  }

  setHost (host) {
    return this.host = host
  }

  open (options, callback) {

  }

  configure () {
    
  }
}
